

export function triangleArea (base: number, height: number) {
    let totalArea: number = (base * height) / 2;

    console.log('El área de un triángulo es:', totalArea);
}

triangleArea (10, 5);

// ? Ejemplo de Fernando:
// let base = 10;
// let height = 5;

// let area = (base * height) / 2;
// console.log('El área es igual a', area);